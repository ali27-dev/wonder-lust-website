<% layout("/layouts/boilerplates") %>
<body>
  <div class="row mt-3">
    <div class="col-8 offset-3">
      <h2>Listing Details</h2>
    </div>

    <div class="card col-6 offset-3 show-card">
      <img
        src="<%= listId.image %>"
        class="card-img-top show-image"
        alt="Listing image"
      />
      <div class="card-body">
        < class="card-text">
          <b><%= listId.title %></b> <br />
          <%= listId.description%> <br />
  &#8360;<%= list.price ? list.price.toLocaleString("en-PK") : "N/A" %> / night <br />
          <%= listId.country%> <br />
          <%= listId.location%> <br />
        </p>
      </div>
    </div>
    <div class="btns">
      <a
        href="/listings/<%= listId._id %>/edit"
        class="btn btn-dark col-1 offset-3 edit-btn"
        >Edit</a
      >

      <form method="post" action="/listings/<%= listId._id %>?_method=DELETE">
        <button class="btn btn-dark offset-5">Delete</button>
      </form>

      <a href="/listings" class="btn btn-dark offset-1">Go Back</a>
    </div>
  </div>
</body>
